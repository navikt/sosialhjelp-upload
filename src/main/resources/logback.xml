<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
            <expression>
                isNull("RUNTIME_ENV") || propertyEquals("RUNTIME_ENV", "local")
            </expression>
        </condition>
        <if>
            <then>
                <encoder>
                    <pattern>%d{YYYY-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [fiksDigisosId=%X{fiksDigisosId} navEksternRefId=%X{navEksternRefId}] - %msg%n</pattern>
                </encoder>
            </then>
            <else>
                <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                    <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                        <inlineHash>true</inlineHash>
                    </throwableConverter>
                </encoder>
            </else>
        </if>


    </appender>

    <appender name="TEAM_LOGS" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>team-logs.nais-system:5170</destination>
        <keepAliveDuration>30 seconds</keepAliveDuration>
        <writeTimeout>1 minute</writeTimeout>
        <reconnectionDelay>1 second</reconnectionDelay>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"google_cloud_project":"${GOOGLE_CLOUD_PROJECT:-}","nais_namespace_name":"${NAIS_NAMESPACE:-}","nais_pod_name":"${NAIS_POD_NAME:-}","nais_container_name":"${NAIS_APP_NAME:-}"}</customFields>
            <includeContext>false</includeContext>
            <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                <inlineHash>true</inlineHash>
            </throwableConverter>
        </encoder>
    </appender>

    <appender name="TEAM_LOGS_LOCAL" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{YYYY-MM-dd HH:mm:ss.SSS} TEAM_LOGS [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="teamlogs" level="INFO" additivity="false">
        <condition class="ch.qos.logback.core.boolex.ExpressionPropertyCondition">
            <expression>
                isNull("RUNTIME_ENV") || propertyEquals("RUNTIME_ENV", "local")
            </expression>
        </condition>
        <if>
            <then>
                <appender-ref ref="TEAM_LOGS_LOCAL"/>
            </then>
            <else>
                <appender-ref ref="TEAM_LOGS"/>
            </else>
        </if>
    </logger>

    <logger name="org.eclipse.jetty" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
    <logger name="io.ktor.server.netty" level="WARN"/>
    <logger name="io.ktor.auth.jwt" level="DEBUG"/>
    <logger name="org.jooq.Constants" level="OFF"/>
</configuration>
