package no.nav.sosialhjelp.upload.database.schema

// Exposed table definitions removed. Use jOOQ-generated classes instead.

// enum class HendelseType remains if used elsewhere

enum class HendelseType {
    DOKUMENTASJON_ETTERSPURT,
    DOKUMENTASJONKRAV,
    SOKNAD,
    BRUKER,
}
